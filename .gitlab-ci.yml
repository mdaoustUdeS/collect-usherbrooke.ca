image: alpine

stages:
  - install
  - download
  - save
  - deploy

installDependency:
  stage: install
  script:
    - apk update && apk add wget

download-usherbrooke.ca/coronavirus/:
  stage: download
  script:
    - ./getUrls.sh

save-change:
  stage: save
  script:
    - echo "Commit will happened here"

pages:
  stage: deploy
  script:
    - ls -la
    - mv html/ public/
  artifacts:
    paths:
      - public
  only:
    - master
